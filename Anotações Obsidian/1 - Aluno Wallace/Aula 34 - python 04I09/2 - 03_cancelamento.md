1 - baixar a pasta cancelamento.csv
2 - vamos analizar um dataset
3 - A empresa Umbrella Corporation contratou desenvolvedores Python (🐍) para analisar um dataset para verificar o alto número de cancelamentos de contratos dessa empresa, e para entregar uma solução para este programa.

Para isso, a Umbrella Corp. forneceu um *dataset* com os detalhes dos cancelamentos. Os Pythonianos (🐍) devem analizar o arquivo utilizado o **Pandas** (🐼).

4 -Vamos começar trazendo todos os dados do dataset, com um arquivo com a extensão .csv
5 - Depois vamos instalar a pasta .venv e ativar ela.
7 - vamos começar instalando uma biblioteca chamada pandas:
8 - depois vamos clicar em kernel no canto superior direito, e instalar o Python com estrelinha.

```python
pip install Pandas
```

9 - células de markdonw são para texto, e células de código para escrever as linhas de código em python.

10 - Para que tudo funcione corretamente, se voltarmos ao projeto depois, temos que executar todos os codigos antes, em run all, ou executar tudo.

![[Pasted image 20260904205619.png]]

11 - nesse dataser alguns cadastros foram feitos de forma errada, analisando os dados a gente consegue perceber:

![[Pasted image 20260904212147.png]]

12 - alguns dados nulos foram registrados.

13- precisamos intalar uma nova biblioteca para exibir gráficos:
```python
pip install plotly.express
```

```python 
pip install nbformat 
```

para subir o código no Github precisa tirar os gráficos em ... limpar celular. ao lado da celular e depois:

```python
ip freeze > requirements.txt
```

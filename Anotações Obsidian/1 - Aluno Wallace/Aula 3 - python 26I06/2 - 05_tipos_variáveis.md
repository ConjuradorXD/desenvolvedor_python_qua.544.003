1 - criar uma nova pasta com o nome: 05_tipos_variaveis.
2 - novo arquivo chamado: main.py.

3 - sempre que definir um input vai ser string, por conta disso precisamos converter essa variável, com os comandos exemplos:

idade = int(input("string")) para int (numero ex: 10)
altura = float(input("string")) para float (para numero quebrado ex:1.80)

4 - Sempre que for usar o float, colocar no final da linha ".replace(",",".") " , isso vai substituir o "." que é obrigatório pela ",".

5 - Para mostrar quando o programa é utilizado em maiúsculo, o comando é : .title()
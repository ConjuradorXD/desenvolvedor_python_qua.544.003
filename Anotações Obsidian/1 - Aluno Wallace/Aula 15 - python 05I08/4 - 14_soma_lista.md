1 - criar uma nova pasta com o nome: 14_soma_lista.
2 - dentro dela o arquivo main.py
3 - Nesse programa vamos trabalhar com uma lista numérica, esse programa vai receber uma lista numérica e vai somar todos os números e vai entregar o resultado, esse programa é muito útil quando vai fazer uma media, que é um assunto de estatística que entra em inteligência artificial.
4 - vamos criar uma lista numérica em uma ordem qualquer:
5 - Em uma lista não é obrigatório colocar virgula, mas é uma boa pratica.
6 - Vamos criar uma variável chamada: total_soma que vai receber uma variável chamada: 

```python
sum(variavel)
```

7 - O comando sum() serve para somar a lista:

8 - Ficando assim:

```python
# Somar lista:

numeros = [10, 15, 12, 8, 26, 100]

total_soma = sum(numeros)

print(f"Total da soma: {total_soma}")
```
